#!/usr/bin/make -f

%:
	dh $@

override_dh_install:
	VERSION=$$(cat $(CURDIR)/VERSION); \
	wget -O smarter https://github.com/smarter-sh/smarter-cli/releases/download/v$$VERSION/smarter-ubuntu-latest-$$VERSION
	chmod +x smarter
	dh_install smarter /usr/bin/
